export const MODE = import.meta.env.MODE;
export type MODE = typeof MODE;
// ApiEndpoint
export const API_ENDPOINT = import.meta.env.VITE_API_ENDPOINT as string;
export type API_ENDPOINT = typeof API_ENDPOINT;
